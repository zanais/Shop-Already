import 'package:flutter/material.dart';

class VendedorProductos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('vendedor'),
    );
  }
}
